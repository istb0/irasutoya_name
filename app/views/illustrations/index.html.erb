<h1>Title</h1>
<div class="container pt-3">
  <!-- いらすと一覧 -->
  <div class="row">
    <div class="col-12">
      <div class="row">
      <% if @illustrations.present? %>
        <%= render @illustrations %>
      <% else %>
        <p>いらすとがありません。</p>
      <% end %>
      </div>
    </div>
  </div>
</div>